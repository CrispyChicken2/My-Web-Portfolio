# The lockfile was resolved with legacy peer deps (vite 8 is ahead of
# @vitejs/plugin-react's declared peer range). This keeps local installs and
# CI (`npm ci`) behaving identically.
legacy-peer-deps=true
